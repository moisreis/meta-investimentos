import type { NextConfig } from "next";

/**
 * Configure the *Next.js* application server.
 *
 * The configuration object holds the options recognized by *Next.js*
 * for compilation, routing, and runtime behaviour.
 */
const nextConfig: NextConfig = {};

export default nextConfig;

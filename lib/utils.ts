/**
 * @summary
 * Re-exports the `cn` utility.
 *
 * @remarks
 * The export exposes `cn` from the `cn` module.
 *
 * @explanation
 * This export provides a local access point for `cn`.
 * It forwards the utility without changing its behavior.
 * Use it where shared class name utilities are imported.
 *
 * @returns The exported `cn` utility.
 *
 * @author Shadcn/ui
 *
 * @date 2026-09-13
 */
export { cn } from "cn";

import { SignInForm } from "@/presentation/components/routes/auth/forms/sign-in.form";

/**
 * The sign-in page.
 *
 * Renders the sign-in form inside the shared *Auth* shell layout.
 */
export function SignInPage() {
  return <SignInForm />;
}

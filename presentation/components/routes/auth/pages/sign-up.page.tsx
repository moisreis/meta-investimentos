import { SignUpForm } from "@/presentation/components/routes/auth/forms/sign-up.form";

/**
 * The sign-up page.
 *
 * Renders the sign-up form inside the shared *Auth* shell layout.
 */
export function SignUpPage() {
  return <SignUpForm />;
}
